{
  "format_version": 1,
  "sksl": {
    "entrypoint": "glow_effect_fragment_main",
    "shader": "// This SkSL shader is autogenerated by spirv-cross.\n\nfloat4 flutter_FragCoord;\n\nuniform vec2 iResolution;\nuniform float iTime;\nuniform float iFrequency;\nuniform float iScale;\nuniform vec4 iBaseColor;\n\nvec4 fragColor;\n\nvec2 FLT_flutter_local_FlutterFragCoord()\n{\n    return flutter_FragCoord.xy;\n}\n\nfloat FLT_flutter_local_sdRoundedRect(vec2 p, vec2 b, float r)\n{\n    vec2 q = abs(p) - (b - vec2(r));\n    return (length(max(q, vec2(0.0))) + min(max(q.x, q.y), 0.0)) - r;\n}\n\nfloat FLT_flutter_local_glowFalloff(float d, float radius)\n{\n    float x = clamp(1.0 - (d / radius), 0.0, 1.0);\n    return x * x;\n}\n\nvec3 FLT_flutter_local_rgb2hsv(vec3 c)\n{\n    float r = c.x;\n    float g = c.y;\n    float b = c.z;\n    float h = 0.0;\n    float s = 0.0;\n    float v = 0.0;\n    float maxC = max(r, max(g, b));\n    float minC = min(r, min(g, b));\n    float delta = maxC - minC;\n    v = maxC;\n    if (maxC <= 0.0)\n    {\n        return vec3(h, s, v);\n    }\n    s = delta / maxC;\n    if (delta > 0.0)\n    {\n        if (r == maxC)\n        {\n            h = (g - b) / delta;\n        }\n        else\n        {\n            if (g == maxC)\n            {\n                h = 2.0 + ((b - r) / delta);\n            }\n            else\n            {\n                h = 4.0 + ((r - g) / delta);\n            }\n        }\n        h /= 6.0;\n        if (h < 0.0)\n        {\n            h += 1.0;\n        }\n    }\n    return vec3(h, s, v);\n}\n\nvec3 FLT_flutter_local_hsv2rgb(vec3 c)\n{\n    float h = c.x;\n    float s = c.y;\n    float v = c.z;\n    if (s == 0.0)\n    {\n        return vec3(v);\n    }\n    float sector = h * 6.0;\n    float i = floor(sector);\n    float f = sector - i;\n    float p = v * (1.0 - s);\n    float q = v * (1.0 - (s * f));\n    float t = v * (1.0 - (s * (1.0 - f)));\n    float r;\n    float g;\n    float b;\n    if (i == 0.0)\n    {\n        r = v;\n        g = t;\n        b = p;\n    }\n    else\n    {\n        if (i == 1.0)\n        {\n            r = q;\n            g = v;\n            b = p;\n        }\n        else\n        {\n            if (i == 2.0)\n            {\n                r = p;\n                g = v;\n                b = t;\n            }\n            else\n            {\n                if (i == 3.0)\n                {\n                    r = p;\n                    g = q;\n                    b = v;\n                }\n                else\n                {\n                    if (i == 4.0)\n                    {\n                        r = t;\n                        g = p;\n                        b = v;\n                    }\n                    else\n                    {\n                        r = v;\n                        g = p;\n                        b = q;\n                    }\n                }\n            }\n        }\n    }\n    return vec3(r, g, b);\n}\n\nvec4 FLT_flutter_local_shade1(vec4 base)\n{\n    vec3 param = base.xyz;\n    vec3 hsv = FLT_flutter_local_rgb2hsv(param);\n    hsv.x = fract(hsv.x + 0.0500000007450580596923828125);\n    hsv.y = clamp(hsv.y * 1.10000002384185791015625, 0.0, 1.0);\n    hsv.z = clamp(hsv.z * 1.0499999523162841796875, 0.0, 1.0);\n    vec3 param_1 = hsv;\n    vec3 rgb = FLT_flutter_local_hsv2rgb(param_1);\n    return vec4(rgb, base.w);\n}\n\nvec4 FLT_flutter_local_shade2(vec4 base)\n{\n    vec3 param = base.xyz;\n    vec3 hsv = FLT_flutter_local_rgb2hsv(param);\n    hsv.x = fract((hsv.x - 0.0500000007450580596923828125) + 1.0);\n    hsv.y = clamp(hsv.y * 0.949999988079071044921875, 0.0, 1.0);\n    hsv.z = clamp(hsv.z * 0.920000016689300537109375, 0.0, 1.0);\n    vec3 param_1 = hsv;\n    vec3 rgb = FLT_flutter_local_hsv2rgb(param_1);\n    return vec4(rgb, base.w);\n}\n\nvec3 FLT_flutter_local_mesh3(vec2 position)\n{\n    float minDim = min(iResolution.x, iResolution.y);\n    vec2 uv = ((position * minDim) + (iResolution * 0.5)) / iResolution;\n    float angle = iTime * 6.283185482025146484375;\n    float cosAngle = cos(angle);\n    float sinAngle = sin(angle);\n    mat2 rotation = mat2(vec2(cosAngle, -sinAngle), vec2(sinAngle, cosAngle));\n    vec2 center = vec2(0.5);\n    float radius = 0.449999988079071044921875;\n    vec2 p0 = center + (rotation * (vec2(1.0, 0.0) * radius));\n    vec2 p1 = center + (rotation * (vec2(-0.5, 0.866025388240814208984375) * radius));\n    vec2 p2 = center + (rotation * (vec2(-0.5, -0.866025388240814208984375) * radius));\n    float epsilon = 9.9999997473787516355514526367188e-05;\n    float w0 = 1.0 / (distance(uv, p0) + epsilon);\n    float w1 = 1.0 / (distance(uv, p1) + epsilon);\n    float w2 = 1.0 / (distance(uv, p2) + epsilon);\n    float wSum = (w0 + w1) + w2;\n    w0 /= wSum;\n    w1 /= wSum;\n    w2 /= wSum;\n    vec3 color0 = iBaseColor.xyz;\n    vec4 param = iBaseColor;\n    vec3 color1 = FLT_flutter_local_shade1(param).xyz;\n    vec4 param_1 = iBaseColor;\n    vec3 color2 = FLT_flutter_local_shade2(param_1).xyz;\n    return ((vec3(w0) * color0) + (vec3(w1) * color1)) + (vec3(w2) * color2);\n}\n\nvec4 FLT_flutter_local_renderGlow(vec2 position, vec2 halfSize)\n{\n    float scale = iScale / min(iResolution.x, iResolution.y);\n    float base = min(halfSize.x, halfSize.y);\n    float thickness = base * scale;\n    float rim = base * scale;\n    float radius = base * scale;\n    float angle = iTime * 6.283185482025146484375;\n    float breathe = mix(0.699999988079071044921875, 1.2999999523162841796875, 0.5 + (0.5 * sin(angle * mix(0.25, 0.60000002384185791015625, iFrequency))));\n    vec2 param = position;\n    vec2 param_1 = halfSize;\n    float param_2 = radius;\n    float dist = FLT_flutter_local_sdRoundedRect(param, param_1, param_2);\n    float rimMask = exp(-pow(abs(dist) / max(rim, 1.0000000133514319600180897396058e-10), 1.2000000476837158203125));\n    float param_3 = abs(dist);\n    float param_4 = thickness;\n    float haloMask = FLT_flutter_local_glowFalloff(param_3, param_4) * 0.89999997615814208984375;\n    float strength = clamp((rimMask * 0.89999997615814208984375) + (haloMask * 0.699999988079071044921875), 0.0, 1.0);\n    strength *= clamp(breathe, 0.800000011920928955078125, 1.0);\n    strength = clamp(strength, 0.0, 1.0);\n    vec2 param_5 = position;\n    vec3 color = FLT_flutter_local_mesh3(param_5) * strength;\n    float alpha = iBaseColor.w * strength;\n    return vec4(color, alpha);\n}\n\nvoid FLT_main()\n{\n    vec2 fragCoord = FLT_flutter_local_FlutterFragCoord();\n    float minDim = min(iResolution.x, iResolution.y);\n    vec2 position_1 = (fragCoord - (iResolution * 0.5)) / vec2(minDim);\n    vec2 halfSize_1 = (iResolution * 0.5) / vec2(minDim);\n    vec2 param_6 = position_1;\n    vec2 param_7 = halfSize_1;\n    fragColor = FLT_flutter_local_renderGlow(param_6, param_7);\n}\n\nhalf4 main(float2 iFragCoord)\n{\n      flutter_FragCoord = float4(iFragCoord, 0, 0);\n      FLT_main();\n      return fragColor;\n}\n",
    "stage": 1,
    "uniforms": [
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 0,
        "name": "iResolution",
        "rows": 2,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 1,
        "name": "iTime",
        "rows": 1,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 2,
        "name": "iFrequency",
        "rows": 1,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 3,
        "name": "iScale",
        "rows": 1,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 4,
        "name": "iBaseColor",
        "rows": 4,
        "type": 10
      }
    ]
  }
}